# CS-Scout v2.1.1-alpha.1 发布说明

这是 2.1.1 的首个公开测试版，重点是把 5E 与完美平台的自动侦察、手动分析和任务控制整理成一致的使用流程。

## 主要更新

- 5E 与完美平台都提供“自动 / 手动”模式。自动模式识别地图和 5 名对手，确认后开始；手动模式可选择地图并输入最多 5 名玩家。
- 5E 自动模式通过本机 5E 客户端检测已匹配对局，并尽量使用精确玩家身份进入原有 Demo 分析流水线。
- 完美平台可自动查找官方组件；非默认安装位置可直接在网页选择目录，并检查数字签名、架构和必需导出。
- 两个平台统一了侧栏文字、按钮状态和确认流程；任务运行时不能切换平台或模式。
- 重新设计左下角任务进度，区分总体完成度、当前阶段、成功数和失败数。
- 分析任务支持取消；失败或取消后可保留当前名单并重新分析。
- 修复快速模式被切回普通模式、任务结束后按钮不可再次点击，以及跨平台任务互相抢占的问题。
- 优化 5E 历史比赛分页、Demo 去重、并行下载与解析调度，减少长时间停留在“解析 Steam ID”的情况。
- 同一比赛的 Demo 在多个玩家之间复用；取消、失败和重复运行时不会留下错误的完成状态。

## Windows 下载

下载以下三个资产：

```text
CS-Scout-Windows-x64-v2.1.1-alpha.1.zip
CS-Scout-Windows-x64-v2.1.1-alpha.1.zip.sha256
SHA256SUMS.txt
```

不要下载 GitHub 自动生成的 `Source code` 压缩包，它不包含完整的运行时地图资源。

完整解压 ZIP 后，依次双击：

```text
windows\Install-CS-Scout.cmd
windows\Start-CS-Scout.cmd
```

首次测试建议每人选择 1 个 Demo、使用普通模式；确认自动识别和回放正常后再提高 Demo 数或使用快速模式。

## 从旧版本更新

1. 等待分析结束或先取消任务，然后关闭旧版本的启动窗口。
2. 把新 ZIP “全部解压”到一个新目录，不要覆盖正在运行的旧目录。
3. 双击新目录中的 `windows\Install-CS-Scout.cmd`。
4. 安装完成后双击 `windows\Start-CS-Scout.cmd`。

Demo 缓存、分析输出和完美平台目录记录保存在 `%LOCALAPPDATA%\CS-Scout`，更换程序目录不会清除这些数据。确认新版本可用后，可以删除旧版本的解压目录。

## Alpha 提醒

- 这是预发行测试版；自动侦察依赖平台客户端当前行为，平台更新后可能需要继续适配。
- 5E 自动模式需要已安装并登录官方 5E 客户端；完美平台模式需要已安装并登录官方完美世界竞技平台。
- 自动侦察不可用时，可切换到同平台的“手动”模式继续分析。
- 请勿公开提交登录 Token、Cookie、签名结果或包含个人会话的数据。

## 验证

- 离线测试覆盖 5E、完美平台、任务取消、失败重试、进度状态、自动/手动切换和前端交互。
- Windows 发布包由严格文件白名单生成，并检查脚本语法、固定依赖、地图资源、敏感文件和 ZIP 清单。
- 发布资产提供 SHA-256 校验文件。
