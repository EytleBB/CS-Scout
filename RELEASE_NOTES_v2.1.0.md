# CS-Scout v2.1.0 发布说明

CS-Scout 2.1.0 在保留原有 5E 分析流程的基础上，加入完美世界竞技平台本地自动侦察，并把两种平台合并到同一套回放界面。

## 主要更新

- 左侧新增“5E / 完美平台”切换，两种平台共用地图、Demo 深度、手枪局合并回放和玩家 Buy 回放。
- 完美平台匹配到对局后自动识别当前地图和 5 名对手；用户名只读，用户确认名单后点击“开始分析”。
- 完美模式直接读取当前 Windows 用户已经登录的完美平台本地会话，不要求手动输入用户名、SteamID 或 Token。
- 匹配成功后即可分析对手已有的历史比赛，不需要等待所有人进入 CS2，也不需要等待当前比赛结束。
- 完美模式并行查询玩家历史、准备 Demo，并根据可用内存使用最多两个解析进程；多人共享同一历史比赛时只下载一次。
- Demo 解析复用同一次事件和回合分类上下文，减少重复解析工作。
- 任一平台正在分析时禁止切换平台，任务结束或失败后自动恢复切换。
- 修复 Demo 缓存清理与磁盘余量准备时序，分析开始前先为下载和解压保留空间。

## Windows 玩家版

下载以下三个资产：

```text
CS-Scout-Windows-x64-v2.1.0.zip
CS-Scout-Windows-x64-v2.1.0.zip.sha256
SHA256SUMS.txt
```

完整解压 ZIP 后，依次双击：

```text
windows\Install-CS-Scout.cmd
windows\Start-CS-Scout.cmd
```

安装器会建立独立 Python 环境并安装固定版本的运行依赖。5E 与完美平台的 Demo 缓存和输出保存在 `%LOCALAPPDATA%\CS-Scout`，升级解压目录时不会自动丢失。

## 完美平台使用条件

- 仅支持 Windows 本地玩家版；需要已经安装并登录完美世界竞技平台。
- 默认读取官方安装目录 `C:\Program Files (x86)\perfectworldarena`；非默认位置可使用 `CS_SCOUT_PWA_DLL` 指定官方 `PvpAlive.dll`。
- CS-Scout 不包含、不修改也不注入完美平台客户端，只读本地日志并调用用户电脑上已经安装的官方签名组件。
- 会话 Token、`Pwa-Jt`、签名和带签名下载地址只在进程内存中使用，不写入回放 JSON 或网页状态。
- 完美接口与回放输出只允许本机回环地址访问。Ubuntu/VPS 部署仍以 5E 服务为主，不能代替玩家本机的完美客户端环境。

完美平台没有公开 Demo API 文档，平台协议或客户端升级后可能需要更新适配。官方公开 FAQ 只说明 Demo 保存期限，没有公布每日固定下载次数；建议合理设置每人 Demo 数并复用本地缓存。

## 验证

- Python 3.11 / 3.12、Windows / Ubuntu CI 测试覆盖 5E 与完美平台离线逻辑。
- Windows 发布包由严格文件白名单生成，并检查 PowerShell 语法、地图与图标签名、固定依赖、路径边界、敏感信息和 ZIP 清单。
- 发布资产同时提供 SHA-256 校验文件。

## 升级提醒

等待当前分析结束后再关闭旧版本。把 2.1.0 解压到新目录并重新运行安装脚本，不要直接覆盖正在运行的旧目录。确认新版本可用后再删除旧程序目录。
