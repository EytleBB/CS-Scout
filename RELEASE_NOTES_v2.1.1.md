# CS-Scout v2.1.1 发布说明

这是 2.1.1 正式版，汇总并封板了四轮公开测试中的 5E / 完美平台双平台工作流、Windows 开箱即用安装和稳定性修复。

## 主要更新

- 5E 与完美平台都提供“自动 / 手动”模式。自动模式识别地图和 5 名对手，用户确认后分析；手动模式可选择地图并输入最多 5 名玩家。
- 5E 自动模式通过本机官方客户端检测已匹配对局，使用精确玩家身份进入原有 Demo 分析流水线；支持非默认安装位置、端口冲突处理和网页选择 `5EClient.exe`。
- 5E 已经运行但没有自动侦察端口时，CS-Scout 会等待用户正常退出后受控重启一次；失败后停止自动重试并保留手动模式，不会循环弹出 UAC。
- 完美平台会自动查找并校验官方组件；手动模式使用当前完美平台会话搜索玩家，支持中文 Steam 昵称全匹配和 SteamID64，同名时不会猜测账号。
- 两个平台统一了侧栏、确认流程和任务状态；任务运行时不能切换平台或模式。
- 分析任务支持取消、失败后修改设置并重试；快速模式、跨平台互斥、Demo 去重、历史比赛分页和并行下载/解析调度已修复或优化。
- 左下角任务进度会显示总体完成度、当前阶段和成功/失败数量。
- 点击另一名玩家时，回放时间轴会回到 0 秒并从头播放。
- Windows 玩家无需预装 Python。缺少兼容的 64 位 Python 3.11/3.12 时，安装器会校验固定 SHA-256 和官方签名后，为 CS-Scout 单独准备 Python 3.12.10；不会修改已有 Python 3.13、PyCharm 或系统 PATH。
- 内置 `Administrator` 或关闭 UAC/管理员审批模式的电脑可以安装和运行；普通管理员账号主动“以管理员身份运行”仍会被阻止。数据始终保存在当前账号的 `%LOCALAPPDATA%\CS-Scout`。

## Windows 下载与更新

下载以下三个资产：

```text
CS-Scout-Windows-x64-v2.1.1.zip
CS-Scout-Windows-x64-v2.1.1.zip.sha256
SHA256SUMS.txt
```

不要下载 GitHub 自动生成的 `Source code` 压缩包。停止旧版，把正式版完整解压到新目录，然后依次双击：

```text
windows\Install-CS-Scout.cmd
windows\Start-CS-Scout.cmd
```

不要覆盖旧版本目录。Demo 缓存、分析结果和平台安装位置记录保存在 `%LOCALAPPDATA%\CS-Scout`，更换程序目录不会丢失。

## 使用提醒

- 5E 自动模式需要已安装并登录官方 5E 客户端；完美平台模式需要已安装并登录官方完美世界竞技平台。
- 自动侦察依赖平台客户端当前行为；平台更新后若暂时不可用，可切换到同平台的“手动”模式继续分析。
- 5E 自动侦察使用仅绑定 `127.0.0.1` 的本机调试端口。使用期间不要运行来源不明的软件，结束后正常退出平台客户端和 CS-Scout。
- 不要公开提交登录 Token、Cookie、签名结果或包含个人会话的数据。

## 验证

- 全量自动测试 259 项通过，覆盖 5E、完美平台、自动/手动模式、任务取消、失败重试、进度、玩家解析和前端交互。
- GitHub CI 覆盖 Windows / Ubuntu 与 Python 3.11 / 3.12。
- Windows 发布包执行严格文件白名单、PowerShell 5.1 语法、固定依赖、地图资源、敏感文件和 ZIP 清单检查。
- 安装器验证私有 Python 下载地址、固定 SHA-256、官方签名、当前用户安装和不修改 PATH/Launcher。
- 发布资产提供独立 SHA-256 校验文件。
